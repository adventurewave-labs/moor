[project]
name = "moor"
version = "1.0.0"
description = "Moor — desired-state control plane for Docker environments"
readme = "README.md"
requires-python = ">=3.11"
license = { text = "MIT" }

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-q"
